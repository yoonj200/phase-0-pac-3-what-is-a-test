const name = "Joe";
const height = 74;
const message = `${name} is ${height} inches tall`; // Interpolation

module.exports = {name, height, message};

// Use console.log, then type "node index.js" into terminal
console.log(message);